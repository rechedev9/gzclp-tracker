# ─── API (apps/api) ──────────────────────────────────────────────────────────

# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/gzclp

# JWT signing secret — use a long random string in production (required)
JWT_SECRET=dev-secret-change-me

# JWT access token expiry (default: 15m)
JWT_ACCESS_EXPIRY=15m

# Allowed CORS origin for the web frontend (default: http://localhost:3000)
CORS_ORIGIN=http://localhost:3000

# API port (default: 3001)
PORT=3001

# Pino log level: trace | debug | info | warn | error (default: info)
LOG_LEVEL=info

# ─── Web (apps/web) ──────────────────────────────────────────────────────────

# API base URL for the web frontend (default: http://localhost:3001)
VITE_API_URL=http://localhost:3001
